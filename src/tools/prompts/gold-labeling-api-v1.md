# Task: initial annotation - API 1.1

Apply the preceding policy to this product. Extract supported semantic features;
abstain when evidence is missing or ambiguous. Return only `{record_id, label}`.
